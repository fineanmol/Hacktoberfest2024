#include <iostream>
using namespace std;

int main(){
    string text;
    cout<<"Enter the text string : ";
    cin>>text;
    string pattern;
    cout<<"Enter the pattern " ;
    cin>>pattern;

    int patt_code[pattern.length()];
    patt_code[0] = 0;
}