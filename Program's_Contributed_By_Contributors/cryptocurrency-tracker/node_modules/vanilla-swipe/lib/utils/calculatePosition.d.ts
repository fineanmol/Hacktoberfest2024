import { EventData, State } from '../types';
export declare function calculatePosition(state: State, options: Options): EventData;
declare type Options = {
    rotatePosition: nextPosition;
    directionDelta: number;
};
declare type nextPosition = {
    x: number;
    y: number;
};
export {};
